[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'python scripts/migrate.py && exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1'"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
